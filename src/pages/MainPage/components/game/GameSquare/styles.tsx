import { Box, styled } from "@mui/material";

export const GameSquareElement = styled(Box)(() => ({
    aspectRatio: "1 / 1",
    width: "100%",
    maxWidth: "70px",

    border: "1px solid gray"
}))